# Program B Checkpoint Execution Plan

## CP-B0 Scope Lock and Semantic Baseline

Goal:

- freeze Program B Release 1 to coordination-ready architectural handoff only,
- confirm launch typologies,
- and lock validation thresholds.

DoD:

- scope lock approved,
- launch typologies frozen to `townhouse` and `villa`,
- pilot and launch thresholds documented.

## CP-B1 Semantic Model and Persistence

Goal:

- add the Program B persistence backbone,
- and generate a first-class semantic coordination model.

DoD:

- persistence exists,
- model resources exist,
- semantic ids are deterministic on benchmark fixtures.

## CP-B2 Quantity and Issue Contracts

Goal:

- generate schedule snapshots,
- add issue registry,
- and expose both through API contracts.

DoD:

- required schedules exist,
- issues are first-class records,
- verification states are visible.

## CP-B3 Coordination IFC Export

Goal:

- generate `architectural_coordination.ifc`
- from the semantic model with launch property sets and validation metadata.

DoD:

- IFC export exists,
- element continuity is preserved for launch entities,
- validation metadata is recorded.

## CP-B4 Handoff Bundle and Manifest

Goal:

- package Program B assets into a releaseable bundle,
- add manifest and readiness summary,
- and register stable object storage assets.

DoD:

- bundle assets exist,
- readiness summary exists,
- release artifact set is frozen by manifest.

## CP-B5 Delivery Workspace and Status UX

Goal:

- turn Program B into visible product value,
- not just backend export logic.

DoD:

- delivery workspace shows readiness, schedules, and issues,
- user-facing status language is clear,
- release controls are wired.

## CP-B6 Downstream Validation Pilot

Goal:

- validate Program B bundles on benchmark `townhouse` and `villa` cases,
- and record continuation feedback from downstream professional review.

DoD:

- benchmark bundles are generated,
- pilot feedback is recorded,
- blocking gaps are triaged.

## CP-B7 Release Readiness and Launch Gate

Goal:

- close launch-blocking gaps,
- validate release language and evidence,
- and finalize Program B Release 1 readiness.

DoD:

- launch thresholds pass,
- scope remains narrow,
- and release evidence is recorded.
